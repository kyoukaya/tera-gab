'use strict'

const defs = require('../data/defs.json')
const config = require('../data/config.json')
const censoredWords = require('../data/censored_words.json')

module.exports = {
    defs,
    config,
    censoredWords
}
